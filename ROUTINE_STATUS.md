# Lang-learner content routine — status

Auto-generated by the scheduled content-drafting routine. Ground truth is always Supabase (`lessons` / `exam_items` tables) plus open `draft-lessons-*` PRs — this file is a snapshot as of the last run, not a source of truth.

## Published (live in Supabase, as of this run)

### Lessons
| id | title | level |
|---|---|---|
| a1-01 | Greetings & Introductions | A1 |
| a1-02 | Numbers 0–100 | A1 |

### Exam items
18 published exam items (ids 1–18), covering the exam-prep track (reading/grammar/vocab CCE-B2-style items). This track is already built per CURRICULUM.md and is not part of the Learn-track drafting this routine does.

## Drafted, merged to main, pending audio + Supabase publish

**PR:** https://github.com/erBhushanPawar/lang-learner/pull/1 (branch `draft-lessons-2026-08-07`) — **merged** to `main`.
File (now on main): `data/drafts/pending-review-2026-08-07.json`

Merging the PR only lands the draft JSON on `main` — it does **not** publish to Supabase. These lessons are still not live.

| id | title | curriculum ref | audio | Supabase |
|---|---|---|---|---|
| a1-03 | Být & mít (to be / to have) | A1 #4 | not yet generated | not yet published |
| a1-04 | Basic word order & questions | A1 #5 | not yet generated | not yet published |
| a1-05 | Nouns & gender (nominative only) | A1 #6 | not yet generated | not yet published |

Remaining before these go live: run `scripts/generate_audio.py` (needs `GOOGLE_TTS_API_KEY`), then run the Supabase publish step (needs service-role key). Both require credentials this automated routine does not have, by design.

**Open question raised in the PR (may still be unresolved — check the PR discussion):** the two already-published lessons (a1-01, a1-02) start at curriculum item 2 ("Greetings"), skipping curriculum item 1 ("Pronunciation & alphabet"). This draft continued from curriculum items 4–6 rather than inserting a pronunciation lesson out of sequence. If not yet answered, confirm whether a dedicated pronunciation & alphabet lesson should be drafted.

## Next up (planned for the following run)

Once a1-03/a1-04/a1-05 are audio-generated and published to Supabase (verify via the live query in step 2 before re-drafting), in curriculum order:

1. **A1 #7 — Adjectives (nominative agreement)** — matching adjective endings to noun gender (natural follow-on to a1-05's gender work).
2. **A1 #8 — Family & people** — vocabulary + possessives (můj, tvůj, jeho, její).
3. **A1 #9 — Food & ordering** — restaurant/café phrases, chtěl bych, prosím, děkuji.

If a1-03/04/05 aren't yet showing as published in Supabase at the next run, do not re-draft them — they're merged to main and just awaiting the manual audio + publish steps.

---
Last run: 2026-08-07 (updated post-merge: PR #1 merged to main; content still pending audio + Supabase publish)
