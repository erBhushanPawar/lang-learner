# Lang-learner content routine — status

Auto-generated by the scheduled content-drafting routine. Ground truth is always Supabase (`lessons` / `exam_items` tables) plus open `draft-lessons-*` PRs — this file is a snapshot as of the last run, not a source of truth.

## Published (live in Supabase, as of this run)

### Lessons
| id | title | level |
|---|---|---|
| a1-01 | Greetings & Introductions | A1 |
| a1-02 | Numbers 0–100 | A1 |
| a1-03 | Být & mít (to be / to have) | A1 |
| a1-04 | Basic word order & questions | A1 |
| a1-05 | Nouns & gender (nominative only) | A1 |
| a1-06 | Adjectives (nominative agreement) | A1 |
| a1-07 | Family & people | A1 |
| a1-08 | Food & ordering | A1 |

Batches 1 (a1-03/04/05, PR #1) and 2 (a1-06/07/08, PR #2) are now confirmed **published** — a human ran audio generation + the publish step (or the CI pipeline succeeded) since the last run; the corresponding draft files have been removed from `main`.

### Exam items
18 published exam items (ids 1–18), covering the exam-prep track (reading/grammar/vocab CCE-B2-style items). This track is already built per CURRICULUM.md and is not part of the Learn-track drafting this routine does.

## ⚠️ Auto-publish CI pipeline — status still needs confirming

`.github/workflows/publish-drafts.yml` + `scripts/publish_draft.py` (added alongside the PR #2 merge) trigger automatically on any push to `main` touching `data/drafts/pending-review-*.json`. If `GOOGLE_TTS_API_KEY` and `SUPABASE_SERVICE_ROLE_KEY` secrets are configured, merging a `draft-lessons-*` PR now auto-generates audio and auto-publishes to Supabase, with no separate manual step.

Since a1-03 through a1-08 are now published and their draft files removed, the pipeline (or an equivalent manual run) has evidently worked at least once since the last run. This routine still cannot verify from read-only access whether that happened via the automated CI path or a manual run — a human should confirm which, and confirm the auto-publish-on-merge behavior is intended, since it changes the safety model this routine was built around ("PR merge ≠ publish").

## Drafted, pending human review

### Batch 3 — new this run

**PR:** https://github.com/erBhushanPawar/lang-learner/pull/3 (branch `draft-lessons-2026-08-09`) — **open**, not yet merged.
File: `data/drafts/pending-review-2026-08-09.json`

| id | title | curriculum ref | audio | Supabase |
|---|---|---|---|---|
| a1-09 | Shopping & numbers in context | A1 #10 | not yet generated | not yet published |
| a1-10 | Directions & places in town | A1 #11 | not yet generated | not yet published |
| a1-11 | A1 review & mini-check | A1 #12 | not yet generated | not yet published |

Remaining before these go live: human review of content accuracy, run `scripts/generate_audio.py` (or let the CI pipeline handle it once its secrets are confirmed/configured), then publish to Supabase.

**Still-unresolved open question (carried over from prior runs):** curriculum item A1 #1 ("Pronunciation & alphabet") has never been drafted. The published/drafted lessons continue from curriculum item #2 (Greetings) onward. A human needs to decide whether a dedicated pronunciation & alphabet lesson should be drafted, and if so, where it slots into the id sequence (a1-00? a renumbering?). This routine has deliberately continued down the roadmap past this gap rather than guessing, consistent with prior runs.

## Next up (planned for the following run)

Once a1-09 through a1-11 are reviewed, audio-generated, and published to Supabase (verify via the live query in step 2 before re-drafting), and assuming the pronunciation & alphabet question above is still unresolved, in curriculum order the entire A1 roadmap will then be drafted — so the next run should move on to **A2**:

1. **A2 #1 — Accusative case** — the direct-object case, most frequently needed case after nominative.
2. **A2 #2 — Genitive case** — possession, negation, quantities ("a lot of", "a little").
3. **A2 #3 — Past tense** — l-participle formation, agreement with subject gender.

If a1-09 through a1-11 aren't yet showing as published in Supabase at the next run, do not re-draft them — check PR #3's status first (it may still be open, or merged-but-not-yet-published). Also re-check whether the pronunciation & alphabet open question has been resolved, and whether the auto-publish-on-merge CI behavior has been confirmed as intended, before continuing further down the list.

---
Last run: 2026-08-09 (drafted a1-09/10/11 as PR #3; confirmed a1-03 through a1-08 are now live in Supabase)
