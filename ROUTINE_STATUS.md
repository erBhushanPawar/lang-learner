# Lang-learner content routine — status

Auto-generated by the scheduled content-drafting routine. Ground truth is always Supabase (`lessons` / `exam_items` tables) plus open `draft-lessons-*` PRs — this file is a snapshot as of the last run, not a source of truth.

## Published (live in Supabase, as of this run)

### Lessons
| id | title | level |
|---|---|---|
| a1-01 | Greetings & Introductions | A1 |
| a1-02 | Numbers 0–100 | A1 |
| a1-03 | Být & mít (to be / to have) | A1 |
| a1-04 | Basic word order & questions | A1 |
| a1-05 | Nouns & gender (nominative only) | A1 |
| a1-06 | Adjectives (nominative agreement) | A1 |
| a1-07 | Family & people | A1 |
| a1-08 | Food & ordering | A1 |
| a1-09 | Shopping & numbers in context | A1 |
| a1-10 | Directions & places in town | A1 |
| a1-11 | A1 review & mini-check | A1 |
| a2-01 | Accusative case | A2 |
| a2-02 | Genitive case | A2 |
| a2-03 | Past tense | A2 |
| a2-04 | Verb aspect, part 1 | A2 |
| a2-05 | Future tense | A2 |
| a2-06 | Modal verbs | A2 |
| a2-10 | Health & body | A2 |
| a2-11 | Housing & daily routine | A2 |
| a2-12 | Locative case | A2 |

**Change since last run:** a2-10, a2-11, a2-12 are now published (they weren't at the last check). All of A1 and A2 items #1–6 and #10–12 are live. A2 items #7–9 and #13–14, and all of B1, are not yet published — see below, this is a CI issue, not a content gap.

### Exam items
18 published exam items (ids 1–18), covering the exam-prep track (reading/grammar/vocab CCE-B2-style items). This track is already built per CURRICULUM.md and is not part of the Learn-track drafting this routine does. No change since last run.

## ⚠️ CI publish issue — human action needed (found this run)

All three previously-open draft PRs (#6, #7, #8) are now **closed**, and git history shows they were in fact **merged to `main`** (merge commits exist; GitHub's API reports `merged:false` only because they were pushed directly rather than merged via the GitHub UI button). All three merges landed within ~30 seconds of each other on 2026-08-14, which fired 3 concurrent runs of `.github/workflows/publish-drafts.yml` — and the concurrency queuing isn't race-safe:

- **PR #6** (`data/drafts/pending-review-2026-08-12.json`: a2-07 Weather & seasons, a2-08 Hobbies & free time, a2-09 Travel & transport) — publish run [31787636561](https://github.com/erBhushanPawar/lang-learner/actions/runs/31787636561) crashed with `ConnectionResetError` mid-TTS-generation for a2-09. **Nothing was published to Supabase.** The draft file is still on `main`, untouched, valid, and ready to retry as-is — no re-drafting needed.
- **PR #8** (`data/drafts/pending-review-2026-08-14.json`: a2-13 Instrumental case, a2-14 A2 review & mini-check, b1-01 Dative case) — publish run [31787616556](https://github.com/erBhushanPawar/lang-learner/actions/runs/31787616556) shows conclusion `cancelled`. Also still on `main`, untouched, valid, ready to retry.
- **PR #7** (`data/drafts/pending-review-2026-08-13.json`: a2-10, a2-11, a2-12) — publish run [31787597714](https://github.com/erBhushanPawar/lang-learner/actions/runs/31787597714) **did** successfully publish all 3 lessons (confirmed live in Supabase above), but the follow-up "remove processed draft file" step lost a `git push` race against the other two concurrent runs (`! [rejected] main -> main (fetch first)`). So this file is a **stale, already-published leftover** still sitting in `data/drafts/` — safe to delete.

**Recommended human action:**
1. Re-run (or trivially re-trigger) the publish workflow for `pending-review-2026-08-12.json` and `pending-review-2026-08-14.json` to actually get a2-07/08/09 and a2-13/14/b1-01 live — their content was already reviewed and merged, it just never successfully published.
2. Delete `data/drafts/pending-review-2026-08-13.json` (already published, orphaned).
3. Consider hardening `publish-drafts.yml`: the `cancel-in-progress: false` concurrency setting queues runs but doesn't protect the final `git push` against a stale checkout — a `git pull --rebase` before pushing (or `git push --force-with-lease` after re-fetching) would fix the race that stranded PR #7's cleanup commit.

**This routine deliberately did NOT re-draft a2-07/08/09 or a2-13/14/b1-01** — that content already exists, was already human-reviewed, and was already merged. Re-drafting would create a second, possibly-divergent copy of already-approved lessons. This routine also did not attempt to touch Supabase, generate audio, or re-run CI itself (out of scope by design).

## Drafted, pending human review

### PR #9 — new this run

**PR:** https://github.com/erBhushanPawar/lang-learner/pull/9 (branch `draft-lessons-2026-08-15`) — **open**, not yet merged.
File: `data/drafts/pending-review-2026-08-15.json`

| id | title | curriculum ref | audio | Supabase |
|---|---|---|---|---|
| b1-02 | All 7 cases — consolidated review | B1 #2 | not yet generated | not yet published |
| b1-03 | Conditional mood | B1 #3 | not yet generated | not yet published |
| b1-04 | Verb aspect, part 2 | B1 #4 | not yet generated | not yet published |

Remaining before this batch goes live: human review of content accuracy, then merge the PR to `main` — that merge triggers the CI publish pipeline (`scripts/publish_draft.py`) which generates audio and publishes to Supabase automatically (see CI issue above before merging — consider merging this one alone, not stacked with retries of #6/#8, to avoid re-triggering the concurrency race). None of these PRs should be auto-merged.

**Still-unresolved open question (carried over from prior runs):** curriculum item A1 #1 ("Pronunciation & alphabet") has never been drafted. The published/drafted lessons continue from curriculum item #2 (Greetings) onward. A human needs to decide whether a dedicated pronunciation & alphabet lesson should be drafted, and if so, where it slots into the id sequence (a1-00? a renumbering?). This routine has deliberately continued down the roadmap past this gap rather than guessing, consistent with prior runs.

## Next up (planned for the following run)

First, check whether the CI issue above has been resolved and whether a2-07/08/09, a2-13/14/b1-01, and b1-02/03/04 (PR #9) have been published. Do not re-draft anything already covered by an open PR or already-merged-but-unpublished draft file sitting in `data/drafts/` — check `data/drafts/` on `main` in addition to open PRs, since this run showed merged-but-unpublished drafts can persist there.

Once B1 #1–4 are live, the next 2–3 lessons in curriculum order are:

1. **B1 #5 — Comparatives & superlatives**
2. **B1 #6 — Expressing opinions & agreement/disagreement** — myslím, že..., souhlasím/nesouhlasím.
3. **B1 #7 — Work & studies vocabulary** — job interviews, CVs, school system basics.

Also re-check whether the pronunciation & alphabet open question has been resolved before continuing further down the list.

---
Last run: 2026-08-15. Confirmed via Supabase: a1-01–11, a2-01–06, and a2-10–12 published (a2-10/11/12 newly landed since last run). Found PRs #6/#7/#8 all merged-but-closed rather than open; diagnosed that PR #6 and #8's publish runs failed (network error / cancelled, respectively) leaving valid, reviewed, unpublished drafts on `main`, while PR #7's cleanup commit lost a git-push race, leaving a stale already-published draft file behind. Did not re-draft that content. Drafted b1-02, b1-03, b1-04 (B1 #2–4) as new PR #9.
